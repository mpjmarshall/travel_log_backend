{
  "note": "Ruled by the human 22 Aug 2026, AFTER plan v3 was written and WHILE the v3 critique was running. Merge into v4. Held in a separate file deliberately: the critic is reading go-backend-plan-v3.json and must not see it change under them.",
  "rulings": [
    {
      "id": "DEC-43",
      "title": "RULED \u2014 S3 access goes through minio-go; the hand-rolled SigV4 presigner is withdrawn",
      "reasoning": "Signing is the one place a bug can pass locally and fail, or sign wrongly, in production: MinIO is a more forgiving verifier than real S3, so a hand-rolled canonical request can look correct for the whole of development. This is the THIRD dependency conversation (after the original six, and Argon2id). The spec's prohibitions name routers, ORMs, JSON and validation libraries \u2014 a storage client is not in that class, and the argument is the same one that admitted pgx.",
      "consequence": "S17 changes shape: the deliverable is no longer ~150 lines of crypto/hmac but a thin wrapper over minio-go behind the same interface. CARRY FORWARD UNCHANGED: the config seam for the public bucket address (a presigned signature still covers the host, so this is unchanged by the swap), content-addressed hex ids, and the client-supplied SHA-256 checksum enforced at the storage layer so a lying client cannot poison the address space. VERIFY BEFORE ADOPTING, because this exact class already bit once: minio-go's own `go` directive against the DEC-27 floor of `go 1.25.0` \u2014 x/crypto silently moved the main module's directive and broke both build and vet. Measure, do not assume.",
      "status": "ruled"
    },
    {
      "id": "DEC-44",
      "title": "RULED \u2014 a short presign expiry, and H1's copy is corrected to match",
      "reasoning": "'Stop sharing' stops new mints at once, but a URL already issued lives until it expires, and H1 says plainly that the link stops working. Rather than hedge the copy to cover a 15-minute gap, the gap shrinks until the plain sentence is true enough to keep.",
      "consequence": "Expiry drops to roughly two minutes, configurable. H1's wording is adjusted so the screen does not overstate. THIS IS CLIENT COPY WORK \u2014 record it in CLIENT-PREREQUISITES.md beside the three sheets whose 'so nothing was deleted' no longer holds for a network write; it is the same class and should be done in one pass. CHECK: the phone caches image BYTES BY OBJECT ID, not by URL, so a short expiry must not evict a cached image \u2014 if it does, the cache design is wrong, not the expiry.",
      "status": "ruled"
    },
    {
      "id": "DEC-45",
      "title": "RULED \u2014 the session token lives in platform secure storage, not in a file",
      "reasoning": "Phase 6 deliberately put the app's documents directory into Files.app so a user could reach their own log. That makes anything stored there readable by anyone holding the phone \u2014 and a session token is a credential, not a preference. The alternatives were the first native platform code in a project whose entire footprint is a five-line MainActivity.kt, or plaintext with a pre-deploy blocker.",
      "consequence": "Keychain on iOS, Keystore on Android, through one package: the SEVENTH dependency, and it gets the conversation the first six had \u2014 recorded here rather than assumed. preferences.json holds settings and never the token. Anything in the plan or in CLIENT-PREREQUISITES.md that assumed a token in preferences.json is now stale. NOTE the pleasing consequence of the earlier reversal: with no device registry (withdrawn with the replication model), the token is the ONLY credential on the device, so this is one decision and not a pattern to repeat.",
      "status": "ruled"
    },
    {
      "id": "DEC-46",
      "title": "RULED \u2014 coverAsset on Trip, City and Place becomes an object id, like Photo.asset",
      "reasoning": "One field carrying two meanings is how a cleanup job deletes live data. It also closes four of the five AssetImage accessors the client's TODO has open, rather than one.",
      "consequence": "Extend the media liveness query to FOUR columns (photos.asset, trips.coverAsset, cities.coverAsset, places.coverAsset) or -sweep-media deletes objects that are live (V3-B6). Extend S12/S13 validation to reject an uncommitted coverAsset exactly as S14 rejects asset. DEC-40's format bump now describes four fields, not one \u2014 say so. The fixture's cover paths need the same rewrite as its photo assets (V3-B7).",
      "status": "ruled"
    },
    {
      "id": "DEC-47",
      "title": "RULED \u2014 two presign lifetimes: short private, longer public",
      "reasoning": "The short window is the revocation knob and belongs on the phone, which re-fetches constantly and caches bytes by object id. The public envelope embeds URLs directly with nothing to re-mint them, so the same window makes a shared page unreadable after two minutes.",
      "consequence": "S3_PRESIGN_TTL_PRIVATE and S3_PRESIGN_TTL_PUBLIC, both from config. THE PUBLIC ONE IS THE HONEST REVOCATION WINDOW and must be what D3's and H1's copy is written against \u2014 not the private one. Add POST /v1/media/mint taking a list of ids so a twelve-photo grid is one round trip, not twelve (V3-SF2). Promote 'ImageProvider keyed by object id' in CLIENT-PREREQUISITES from a note to REQUIRED, with the reason: NetworkImage's equality is (url, scale), so a re-mint is a different cache key and Flutter's ImageCache misses every time.",
      "status": "ruled"
    },
    {
      "id": "DEC-48",
      "title": "RULED \u2014 the auth routes get a rate limit and an Argon2 concurrency cap in v1, at S10",
      "reasoning": "Ten lines, and the limiter plus client-IP resolution already exist from S04. The 64 MiB-per-login ceiling is the plan's most-repeated measurement and it currently guards nothing.",
      "consequence": "AUTH_RATE_LIMIT_PER_MIN per client address on register and session, plus a buffered-channel semaphore capping concurrent argon2.IDKey calls. REJECT with 429 rather than queueing \u2014 queueing converts memory exhaustion into timeout exhaustion. One leg: the N+1th concurrent login is refused, not made to wait.",
      "status": "ruled"
    },
    {
      "id": "DEC-55",
      "title": "RULED \u2014 V4 \u00a71 applies to this plan: a vertical slice is built BEFORE the remaining steps are executed",
      "reasoning": "agent-graph-spec-V4 \u00a71 says a plan over eight steps is planned to the first running thing, then re-planned. This plan is 23 steps and is the run that produced that rule. Declining to apply it here would be the clearest possible evidence the rule will not be followed anywhere.",
      "consequence": "THE SLICE IS A VERTICAL CUT, NOT THE FIRST N STEPS. V4 \u00a71: 'a scaffold with no behaviour' does not count, nor does 'a schema with no query', nor anything whose acceptance check is 'the file exists'. It must RUN, be checked in, and exercise the real stack rather than a mock. Target arc: docker compose up -> register -> sign in -> PUT one trip -> GET /v1/logbook -> the trip comes back -> restart the stack -> it is still there. That last leg is the one that proves the volume, and it is the failure that hides until a redeploy. It therefore takes PARTS of S01, S03, S05, S06 (a few tables, not twelve), S08 (including both transaction helpers, because the slice is where the bump/no-bump split is first testable), S09, S10, S13 (trips only) and S22 (api + postgres; no bucket, no Caddy). WHAT THE SLICE DELIBERATELY OMITS, and each is a decision the slice is expected to inform rather than settle: object storage and all media routes, the public share path, the cascades, and the ETag/304 machinery beyond the version counter existing. AFTER THE SLICE the remaining plan is RE-PLANNED with the slice as entry_state \u2014 it is not resumed. Anything the slice contradicts is struck with the contradiction recorded; anything it validates is inherited.",
      "status": "ruled"
    },
    {
      "id": "DEC-56",
      "title": "RULED \u2014 the four client copy sentences are deferred to the client phase, not fixed now",
      "reasoning": "The replacement wording should be written against failure modes that exist rather than guessed at and revised. The four are D1, D2 and D3's 'so nothing was deleted/removed/changed' and D3's 'the shared link stops working'.",
      "consequence": "Stays in CLIENT-PREREQUISITES as one item covering all four, with the note that D3's is the consequential one \u2014 it sits on the sheet where the user types a trip name to destroy it. Not scheduled into any backend step.",
      "status": "ruled"
    },
    {
      "id": "DEC-57",
      "title": "RULED \u2014 the three city foreign keys are ON DELETE RESTRICT",
      "reasoning": "`places.city_id`, `photos.city_id` and `walks.city_id` had no settled delete action because the client has no delete-a-city control, so no sheet copy exists to derive one from. Three arguments, in order of weight. (1) THE FAILURE MODES ARE NOT SYMMETRIC: RESTRICT -> CASCADE later is a one-line migration; CASCADE -> RESTRICT after it has silently destroyed photographs is not recoverable. (2) A cascade here would be the LARGEST destructive act in the app \u2014 every place in the city, every photograph taken there across every trip, every walk \u2014 against a trip deletion that itemises four consequences and makes the user type the trip's name. Nothing authorises it. (3) It makes the missing decision visible at the right moment: whoever adds a delete-a-city control is stopped by the database exactly when they should be writing the sheet copy.",
      "consequence": "Three FKs at `ON DELETE RESTRICT` in migration 0001, each carrying a comment saying it is restrictive BECAUSE no user-facing sentence authorises a cascade \u2014 so the next reader knows it is a decision rather than a default. RESTRICT rather than NO ACTION: it checks immediately and states the intent. This closes the first of the diagram's flagged items and removes UNSETTLED from the relationship table. It is consistent with the app's existing instinct \u2014 deleting a trip already KEEPS its places and its cities. TRIGGER for revisiting: a delete-a-city control, at which point the sheet copy is written first and the FK follows it, which is the order every other cascade in this app was decided in.",
      "status": "ruled"
    },
    {
      "id": "DEC-58",
      "status": "ruled",
      "title": "RULED \u2014 the media reference is a real FOREIGN KEY on all four columns, not a Go check",
      "reasoning": "A Go check can be bypassed by the next route somebody adds and nothing notices; a database constraint cannot. This is the same argument the plan already makes for the delete cascades \u2014 the schema is the specification \u2014 applied to the one reference that was going to be the exception.",
      "consequence": "FKs from `photos.asset`, `trips.cover_asset`, `cities.cover_asset` and `places.cover_asset` to `media_objects(traveller_id, id)`, composite on traveller_id. The three cover columns are NULLable and an FK on a nullable column is satisfied by NULL, so nothing is forced to carry a cover. ORDERING FALLS OUT OF THIS and must be stated: an object must be COMMITTED before any row can reference it, which the upload flow already does (begin -> PUT -> commit -> reference) \u2014 but it means S16's committed-asset validation is now enforced twice, in Go for the 422 message and in the schema as the guarantee. Keep both: the Go check exists to produce `422 invalid_field`, not to be the guard. The sweep is UNAFFECTED \u2014 its grace window covers objects uploaded but not yet referenced, and an FK never blocked those; what it now blocks is deleting an object something still points at, which is what the sweep was hand-rolling a liveness query to avoid. Re-derive the FK count; do not increment it."
    },
    {
      "id": "DEC-59",
      "status": "ruled",
      "title": "RULED \u2014 country stays flattened into `cities`; there is no `countries` table",
      "reasoning": "The client has no country concept \u2014 no input, no screen, no list \u2014 and derives it from the city. A lookup table would invent an entity nothing asks for. Same instinct that refused to invent a cascade for city deletion (DEC-57): this app does not add what no surface authorises.",
      "consequence": "`cities.country_code` (ISO-3166-1 alpha-2, regexp-validated) and `cities.country_name` stay as two columns. TRIGGER for revisiting: a country-level feature \u2014 browsing by country, counting them, a flag or a map. Normalising later is one migration and a join; inventing the table now is a permanent join for two strings."
    },
    {
      "id": "DEC-60",
      "status": "ruled",
      "title": "RULED \u2014 email is `text`, lowercased on write, with a unique index; NOT `citext`",
      "reasoning": "`citext` needs a Postgres extension, which in this project is a dependency decision of the same class as adding a library \u2014 and the type is increasingly discouraged in favour of collation-based approaches. Lowercase-on-write is explicit rather than magic.",
      "consequence": "`email text NOT NULL` with `UNIQUE`. Exactly TWO write sites lowercase it \u2014 register and sign-in lookup \u2014 and both are named so the rule cannot be applied at one and forgotten at the other. Add the leg: register `A@B.com`, sign in as `a@b.com`, and assert it resolves; and register twice with different casing and assert the second answers 409. That second leg is the one that fails if a future write site forgets."
    },
    {
      "id": "DEC-61",
      "status": "ruled",
      "title": "RULED \u2014 register takes {email, passphrase} only, and does NOT mint a session",
      "reasoning": "One route, one job. Putting `name` on register creates a second way to set a name beside `PATCH /v1/me`, and two writers of one field drift. Not auto-signing-in keeps the sign-in path exercised from the very first launch rather than making it a branch only reached on the second \u2014 which is exactly the code that breaks quietly.",
      "consequence": "`POST /v1/auth/register` body `{email, passphrase}` -> 201 with the traveller, no token. `POST /v1/auth/session` body `{email, passphrase}` -> the opaque token. `traveller.name` is NULL until `PATCH /v1/me` sets it \u2014 which the client's own model already tolerates: `sample_log.dart` says a missing `traveller` key 'reads as a log nobody has named yet'. CLIENT CONSEQUENCE, record it in CLIENT-PREREQUISITES: the sign-up flow is two requests, and the first screen after registering must handle a traveller with no name \u2014 which is a state U1 can already render. The wrong-passphrase and unknown-email responses stay byte-identical (no enumeration oracle); that leg already exists and is unaffected."
    },
    {
      "id": "DEC-62",
      "status": "ruled",
      "title": "RULED \u2014 adopt the layering from 'Building a Clean REST API in Go, no frameworks': a Service type between handlers and the store",
      "reasoning": "The human first proposed amitshekhariitbhu/go-backend-clean-architecture and, on being shown that its stack violates four spec constraints by name (Gin at L18, MongoDB at L20, JWT at L24, Viper at L30), rejected it and supplied this instead. This reference is stdlib-only over `http.ServeMux` and fights nothing in the spec. The plan was ALREADY most of this shape \u2014 the article's central inversion, 'the business logic owns the contract and the storage implementation satisfies it', is exactly what `internal/logbook/store.go` declaring the Store interface already does.",
      "consequence": "ONE REAL ADDITION: a `Service` type owning business rules, so handlers translate HTTP and nothing more. Take the ARTICLE'S form, not the Clean Architecture repo's \u2014 a struct beside the model, NOT a fifth directory. That is the seam without the ceremony, and the ceremony is the standard criticism of the heavier form. IT MUST EARN ITS PLACE PER OPERATION. Most of the 24 routes are plain CRUD where a service method forwards to the repository and returns \u2014 that file is noise. The three operations that genuinely need it are the compound ones: `refilePhoto` (moves a pin AND creates-or-reuses an occasion, one transaction, four documented rules), `removePlace` (two branches, and a statement ORDER that silently inverts D2's promise if reversed), and the media commit flow. Write the service for those; let simple CRUD go handler->store directly, and say so rather than adding empty forwarding methods for symmetry. TYPED SENTINEL ERRORS mapped centrally to status codes is the article's error idiom and it COMPOSES with DEC-12's closed code vocabulary rather than replacing it: the sentinel is the domain's word, the code is the wire's. One mapping function, and DEC-12's AST sweep still polices the wire side. UNCHANGED: `http.ServeMux`, `database/sql`, PostgreSQL, opaque tokens, os.Getenv, and every other spec constraint. NOTHING already built is invalidated \u2014 VS1 and VS2 stand."
    },
    {
      "id": "DEC-63",
      "status": "ruled",
      "title": "RULED \u2014 every foreign key's CHILD column gets an index",
      "reasoning": "PostgreSQL does not index the child side of an FK automatically. Every ON DELETE CASCADE and every RESTRICT check therefore sequentially scans the child table. This is the most common real-world Postgres omission and it was in the schema by silence, not by decision.",
      "consequence": "Indexes on: visits(traveller_id, place_id), visits(traveller_id, trip_id), photos(traveller_id, trip_id), photos(traveller_id, place_id), photos(traveller_id, visit_id), photos(traveller_id, city_id), walks(traveller_id, trip_id), walks(traveller_id, city_id), places(traveller_id, city_id), share_links(traveller_id, trip_id), sessions(traveller_id), and the four media reference columns (DEC-58). LEAD WITH traveller_id: every query is tenant-scoped, so a composite starting with it serves both the FK check and the ordinary read. HOW IT IS PROVEN, and this is the part that makes it more than a hunch: EXPLAIN the cascade before and after and assert the plan changes from Seq Scan to Index Scan. An index nobody proved is used is an index that may not be."
    },
    {
      "id": "DEC-64",
      "status": "ruled",
      "title": "RULED \u2014 `trips.city_ids` becomes a JOIN TABLE; DEC-26's jsonb choice is REVERSED",
      "reasoning": "DEC-26 chose jsonb to avoid Postgres array support through `database/sql`. That reason does not survive scrutiny: A JOIN TABLE NEEDS NO ARRAY SUPPORT AT ALL \u2014 every column is a plain scalar. So jsonb bought nothing and cost referential integrity, which had to be re-implemented in Go with its own 422, its own leg and its own mutation proof. It also cost the reverse lookup (which trips include this city), which the client's 'Third visit' and P1 year rows lean on.",
      "consequence": "`trip_cities(traveller_id, trip_id, city_id, ordinal)` with real FKs to trips and cities, PK (traveller_id, trip_id, city_id), and a UNIQUE on (traveller_id, trip_id, ordinal) so the order cannot silently duplicate. ORDERING IS PRESERVED AND IT IS LOAD-BEARING \u2014 the client's cityIds is an ordered list in travel order; emit `ORDER BY ordinal`, never natural order. DELETES: both FKs CASCADE \u2014 removing a trip removes its rows, and DEC-57's city RESTRICT now has a real child to protect. WHAT THIS DELETES: the Go referential check for city_ids, its 422 and its mutation proof all go \u2014 the schema does that job now. Say so rather than leaving dead guards. The wire shape is UNCHANGED: the client still sends and receives `cityIds` as an ordered JSON array; only the storage changes."
    },
    {
      "id": "DEC-65",
      "status": "ruled",
      "title": "RULED \u2014 a UNIQUE index on lower(email); SUPERSEDES DEC-60's lowercase-on-write rule",
      "reasoning": "DEC-60 correctly refused citext (a Postgres extension is a dependency decision in this project) but settled on an application rule enforced at two call sites. A functional unique index needs no extension AND is enforced by the database, so it is strictly better than both options that were offered. My omission, not the human's.",
      "consequence": "`CREATE UNIQUE INDEX travellers_email_lower_key ON travellers (lower(email))`, and the plain UNIQUE on the column is DROPPED \u2014 keeping both means a case variant fails on the wrong constraint with the wrong message. Lookups MUST use `WHERE lower(email) = lower($1)` or the index is not used \u2014 assert that with EXPLAIN, not by reading. STORE THE ADDRESS AS THE USER TYPED IT: the index makes lowercasing on write unnecessary, and the local-part of an email address is case-sensitive per RFC 5321 even though no real provider treats it so. DEC-60's SECOND LEG SURVIVES AND IS NOW STRONGER: register twice with different casing and assert 409 \u2014 it now fails at the database rather than depending on a call site remembering."
    },
    {
      "id": "DEC-66",
      "status": "ruled",
      "title": "BLOCKER FIX \u2014 composite ON DELETE SET NULL must name its columns; PostgreSQL 15 becomes a hard floor",
      "reasoning": "EXECUTED on PostgreSQL 17.11: a composite FK's `ON DELETE SET NULL` nulls EVERY column of the referencing key, including `traveller_id`, which is NOT NULL. Postgres echoed its own generated statement: `UPDATE ONLY photos SET traveller_id = NULL, place_id = NULL`. So D2's keep branch and `_repointed` \u2014 two of the seven cascades the schema exists to implement \u2014 abort rather than clearing a pin. THREE REVIEW PASSES MISSED IT because the D3 leg passes over the defect: in that path the photograph is cascade-deleted by photos.trip_id before the broken FK fires.",
      "consequence": "`ON DELETE SET NULL (place_id)` and `ON DELETE SET NULL (visit_id)` \u2014 the column-list form, PostgreSQL 15+. Verified on 17.11: traveller_id survives and place_id AND visit_id clear together, which is exactly `Photo.copyWith(clearPlace: true)`. POSTGRESQL 15 IS NOW A HARD FLOOR. State it in the migration header AND make `make test-db` REFUSE an older server \u2014 compose pins 17, but a developer's local 14 would reproduce the blocker with no clue why. S15's ordering leg must land AFTER this fix: today the wrong order raises a NOT NULL violation rather than silently keeping the photographs, so the mutation proof would redden for the wrong reason, which DEC-28 forbids. Assert on the SURVIVING ROW COUNT, never on error/no-error."
    },
    {
      "id": "DEC-67",
      "status": "ruled",
      "title": "RULED \u2014 share links keep their history, with a PARTIAL UNIQUE INDEX enforcing one live link",
      "reasoning": "The PK was never specified and the two candidates behave differently. EXECUTED: with `PRIMARY KEY (traveller_id, trip_id)`, H1's 'Stop sharing' then 'New link' fails outright \u2014 `duplicate key value violates unique constraint`. And the class diagram asserts '0..1 LIVE' which NEITHER candidate enforces.",
      "consequence": "`PRIMARY KEY (traveller_id, token)`, plus `CREATE UNIQUE INDEX share_links_one_live ON share_links (traveller_id, trip_id) WHERE revoked_at IS NULL`. The partial index is the ONLY thing that enforces the cardinality the diagram already claims, and it needs no extension. 'New link' becomes: revoke the current row, insert a new one \u2014 same transaction. The revocation record survives, which matters because DEC-10 stores the token in plaintext: history now shows which token was live when."
    },
    {
      "id": "DEC-68",
      "status": "ruled",
      "title": "RULED \u2014 `visits.at` is `timestamptz`, not `date`",
      "reasoning": "The wire carries a real time (`2027-12-06T07:05:00.000Z`) and `date` truncates it silently. Two losses: DEC-26's refile ordinal rule cannot break a tie between two visits on the same day \u2014 the exact Kyoto-in-May shape the fixture is built around \u2014 and a date-only string makes the client's `DateTime.parse` return a NON-UTC local time, unlike every other date in the log.",
      "consequence": "`visits.at timestamptz NOT NULL`. `trips.start`, `trips.ended_on` and `walks.recorded_on` stay `date` \u2014 those genuinely are midnight-UTC on the wire and `date` is lossless. BUT THE EMITTER MUST RE-RENDER THEM as `T00:00:00.000Z` or the same parse asymmetry appears there instead. One golden round-trip leg PER DATE-BEARING FIELD, asserting the emitted string is byte-identical to what the client sent."
    },
    {
      "id": "DEC-69",
      "status": "ruled",
      "title": "RULED \u2014 `trips.end` is renamed `ended_on`; `trip_cities.city_id` becomes RESTRICT",
      "reasoning": "`end` is a RESERVED PostgreSQL keyword \u2014 `SELECT id, start, end FROM trips` is a syntax error, and in a project with no ORM every hand-written query must remember the quotes forever. And DEC-64 claimed CASCADE on trip_cities.city_id gave DEC-57's RESTRICT 'a real child to protect'; EXECUTED, it does the opposite \u2014 a city with no places or photos deletes silently and vanishes from every trip's list, leaving an ordinal gap.",
      "consequence": "`started_on` and `ended_on` in the schema; THE WIRE KEYS STAY `start` AND `end` and the emitter maps them, so nothing in the client or the approved diagrams changes. Free now, a migration plus every query later. `trip_cities.city_id` -> RESTRICT, consistent with the other three city FKs. `trip_cities.trip_id` STAYS CASCADE \u2014 that one is read off D3. DEC-64's 'a real child to protect' sentence is now TRUE rather than aspirational."
    },
    {
      "id": "DEC-70",
      "status": "ruled",
      "title": "CORRECTION to DEC-63 \u2014 the rule is 'a LEADING PREFIX of some index', derived from the catalog",
      "reasoning": "DEC-63 said 'every FK child column gets an index' and, applied literally, did two wrong things at once: it MISSED `trip_cities(traveller_id, city_id)` \u2014 DEC-64's new table, where city_id is third in the PK and cannot serve the FK \u2014 and it DUPLICATED `share_links_pkey` exactly. The missing-index class reappeared inside the ruling written to eliminate it.",
      "consequence": "RESTATED: every FK child column must be a LEADING PREFIX of some index. Derive the list from `pg_index.indkey`, never from the FK list. Add `trip_cities_city_idx`; drop the `share_links` duplicate. AND THE PROOF METHOD FROM DEC-63 IS WITHDRAWN \u2014 it cannot work. RI checks run in C-level triggers using internal SPI plans and NEVER appear in EXPLAIN, not even under ANALYZE. Replace with: (a) a CATALOG leg asserting every FK child column is a leading prefix of some index \u2014 true at any size, and the one that matters now; (b) `SET enable_seqscan=off` proving each index is USABLE. RECORD THE HONEST LIMIT in EVIDENCE.md: at 284 rows the planner correctly declines almost all of these \u2014 measured, exactly one index was chosen during a full trip cascade \u2014 so nobody later 'fixes' a passing test by deleting an index that is right for the day the log grows."
    },
    {
      "id": "DEC-71",
      "status": "ruled",
      "title": "RULED \u2014 three package renames: internal/api -> internal/rest, internal/store -> internal/postgres, internal/objects -> internal/media",
      "reasoning": "Three naming problems, one of them a real collision. (1) `cmd/api` is the BINARY (the Go convention) and `internal/api` was the HTTP handlers \u2014 two different things sharing one word, so 'the api package' is ambiguous in every import and every conversation. (2) `store.Store` STUTTERS, which Go's style guide calls out because the package name is part of the identifier \u2014 and it is worse here than usual, because the interface it implements is declared in `internal/logbook` and is ALSO called `Store`, so `logbook.Store` and `store.Store` would sit side by side meaning interface and implementation. (3) `objects` is S3's vocabulary; `media` is the domain's own word and matches the `media_objects` table.",
      "consequence": "`internal/rest` (handlers and routes), `internal/postgres` (the database implementation), `internal/media` (photograph storage). `internal/httpx` is DELIBERATELY UNCHANGED \u2014 the `x` suffix is a recognised Go idiom for plumbing extensions and it draws a real boundary: httpx is envelope, errors and middleware; rest is handlers. TIMING: ruled while VS4 was mid-flight creating `internal/store`; the rename was sent to that worker to apply in place rather than land as a follow-up commit. The other two packages do not exist yet, so both are free. NOTHING ELSE MOVES: no table names, no wire keys, no behaviour. The database naming was reviewed at the same time and is correct as it stands \u2014 plural snake_case tables, snake_case columns with the camelCase wire mapped in the emitter, and explicit constraint names with `_fk`/`_uq` suffixes so catalog tests assert on stable names rather than PostgreSQL's positional ones."
    },
    {
      "id": "DEC-72",
      "status": "WITHDRAWN \u2014 superseded by DEC-73",
      "title": "WITHDRAWN \u2014 the Standard-Go-Project-Layout move (deploy/ -> deployments/, Dockerfile -> build/package/). Never applied.",
      "reasoning": "go_backend.md L17 names the Standard Go Project Layout explicitly. We deviated from it in three places and recorded NONE of them as a divergence \u2014 unlike the golang:1.26 build stage and the fourth compose service, which both are. That inconsistency is the defect: the divergence register is only trustworthy if it is complete. HONEST COUNTER-ARGUMENT, recorded so nobody re-litigates it later: that layout is a community repository, not an official Go standard, and the Go team has distanced itself from it. A flatter shape is common in production Go. So this is 'diverges from the document the spec named', not 'wrong'. The human chose to follow the named document, which makes the register complete without needing a new entry.",
      "consequence": "Withdrawn before application, on the human's question 'does that sound like normal human naming?'. It does not. `build/package/Dockerfile` and `deployments/` are among the MOST WIDELY IGNORED parts of that layout \u2014 open almost any real Go service and the Dockerfile is at the repo root. DEC-72 optimised for 'matches the document the spec named' at the cost of 'looks like what a person would do', and I recommended it without flagging that those conflict here. Superseded by DEC-73."
    },
    {
      "id": "DEC-73",
      "status": "ruled",
      "title": "RULED \u2014 the Dockerfile moves to the REPO ROOT; deploy/ keeps its name; recorded as a deliberate divergence from the layout the spec names",
      "reasoning": "go_backend.md L17 names the Standard Go Project Layout. Its `build/package/` and `deployments/` conventions are widely ignored in practice, and following them literally reads as over-application of a document the Go team itself has distanced from. The project's other divergences (the golang:1.26 build stage, the fourth compose service) are recorded rather than hidden, so this becomes the THIRD recorded divergence rather than an unrecorded deviation \u2014 which is what made the register untrustworthy in the first place.",
      "consequence": "`Dockerfile` at the REPO ROOT \u2014 where a Go developer looks first, and where the build context already is, so `build.context: .` and `dockerfile: Dockerfile` both simplify. `deploy/` KEEPS ITS NAME (compose, .env.example). `migrations/` stays at root. `.dockerignore` was ALREADY at the repo root and does not move \u2014 it belongs to the context, not to the Dockerfile. RECORD IT: a third entry in DIVERGENCES.md naming L17, what was done instead, and why. STILL TOUCHES the Makefile, compose's `build` keys, and the `test/image` legs that name the Dockerfile path literally. Apply once VS4 and the sweep worker are clear; verify with `make check` AND `make test-image`, and confirm the shipped image is unchanged \u2014 two layers, binary plus CA bundle."
    },
    {
      "id": "DEC-74",
      "status": "ruled",
      "title": "RULED \u2014 the handlers package is `internal/httpapi`, not `internal/rest`",
      "reasoning": "`rest` names an architectural style rather than the contents, which is slightly unusual as a Go package name. `httpapi` is what the reference article the human chose (DEC-62) actually uses, and it is plain. Supersedes the `internal/rest` half of DEC-71; the other two renames in DEC-71 (`postgres`, `media`) stand.",
      "consequence": "`internal/httpapi` \u2014 handlers and routes, imports the domain. `internal/httpx` UNCHANGED and the boundary is unchanged: httpx is envelope, error vocabulary, middleware and rate limiting, and imports NO domain; httpapi knows what a trip is. Two HTTP packages with one clear line between them. The package does not exist yet, so this costs nothing. It lands at VS7."
    },
    {
      "id": "DEC-75",
      "status": "ruled",
      "title": "RULED \u2014 the backend is seeded by `make seed` from the captured client fixture; the client ships with NO mock data",
      "reasoning": "The human is deleting `sample_log.dart` and `assets/imagery/` from the Flutter app entirely \u2014 'No mock files. Kill them.' \u2014 so the app's empty states become the real first-run experience rather than a branch nobody could reach. That leaves the backend with nothing to work against, and leaves every Go round-trip test with no reference document. CAUGHT IN TIME: the 85,422-byte encoded fixture existed ONLY in a scratch directory. The Go repo had never captured it, and `internal/logbook/` does not exist yet. Once the Dart source is deleted it can never be regenerated. Copied to `internal/logbook/testdata/client_sample_log.json`, sha256 03e88872\u2026 matching the figure the fix verifier recorded independently.",
      "consequence": "`make seed` loads that document into Postgres. It is the right seed BECAUSE it is a real logbook shape rather than three rows of foo: 7 trips, 12 cities, 17 places, 49 visits, 284 photographs, 2 walks \u2014 and it carries the edge cases the schema was designed around. Kyoto visited three times (the ordinal tie DEC-68 needed timestamptz for), a wishlist place with no visits (which D3 promises survives a trip deletion), 28 visits dated before the trip that names them, and only TWO distinct asset locators across all 284 photographs \u2014 which is what makes content-addressed storage collapse the whole library to two objects. AS-IS, NOT ANONYMISED: the edge cases are the value, and rewriting the names would risk flattening exactly the shapes that found defects. IT IS A DEV COMMAND AND MUST SAY SO \u2014 never run at boot, never in production, and the target refuses against a non-empty log rather than merging. NOTE THE ASYMMETRY AND KEEP IT: mock data on the SERVER, run deliberately by a developer, is not the same thing as an app that ships fake trips baked in. The first is a fixture; the second is the 'stranger's data' objection the client's own record makes."
    },
    {
      "id": "DEC-76",
      "status": "ruled",
      "title": "RULED \u2014 the CLIENT bumps to logbookFormatVersion 2; the server keeps emitting 2 only",
      "reasoning": "MEASURED by the re-plan, and neither side had noticed: the server stamps `\"version\": 2` and `Emit` refuses anything else, while the client's `decodeLogbook` compares `version != 1` with EXACT EQUALITY and quarantines. `X-Logbook-Format: 1` answers 406 because `Formats()` is `[2]`. THERE IS NO VERSION AT WHICH THE TWO CAN EXCHANGE A DOCUMENT. DEC-40's 'costs nothing today because nothing has shipped' was true of the backend and false of the client, which has shipped.",
      "consequence": "`logbookFormatVersion` -> 2 on the client. Justified on its own terms rather than to match the server: `Photo.asset` and the three `coverAsset` fields change MEANING from a bundled path to an object id (DEC-46, DEC-58), and the client's own rule is to move the version when an old file's meaning changes. NO SERVER CHANGE. Emitting 1 as well would mean maintaining a shape nothing asks for. THE CACHE MAKES THE BUMP CHEAP, and that is the reason it is safe: under DEC-34 a cache the client cannot read is DELETED AND RE-FETCHED, not quarantined. A version bump costs one refetch, where against a file-as-record it would have cost the log. RECORD IN CLIENT-PREREQUISITES: this is a client change and it must land before the first real request, not after."
    },
    {
      "id": "DEC-77",
      "status": "ruled",
      "title": "RULED \u2014 the two fixture PNGs are captured into the backend repo",
      "reasoning": "All 284 fixture photographs and every `coverAsset` point at one of two files, and they exist only on the client's `main` branch \u2014 `wipe/mock-data` deleted them and `pubspec.yaml` declares no assets at all. Substituting different bytes would change every content hash in the plan.",
      "consequence": "CAPTURED at `internal/logbook/testdata/imagery/` \u2014 `hero-mountain.png` (sha256 e66b552e6043510b\u2026) and `card-ireland.png` (8dfb203bc0f89065\u2026), 1.0 MB. Taken from client `main` before that branch is merged away, the same rescue the encoded logbook needed. They are TEST DATA, not app assets: the client ships no imagery now, and these exist so `make seed` can put real bytes in the bucket and the content addresses in the plan stay true."
    },
    {
      "id": "DEC-78",
      "status": "ruled",
      "title": "RULED \u2014 the compose service is `postgres`; media before seed",
      "reasoning": "CLAUDE.md flagged that the slice plan said `postgres` and the old plan said `db`, and asked the re-plan to settle it. `postgres` is what is running and what `DATABASE_URL` names. Separately, the re-plan flagged its own media-before-seed ordering as its most challengeable call; confirmed, because the seed needs media rows to point at and stub references would have to be rewritten.",
      "consequence": "`postgres` everywhere. Order stands: R2 media, R3 media routes, R4 seed."
    },
    {
      "id": "DEC-82",
      "status": "ruled",
      "title": "RULED \u2014 migration 0002 BACKFILLS the share flags on trips written before it",
      "reasoning": "EXECUTED by the database lens: after the two ALTERs, pre-existing rows stay f|f|f while every new row reads t|t|f, and nothing can distinguish 'written before 0002' from 'the user turned sharing off'. The server wrote a default the CLIENT NEVER HAD \u2014 the client's own defaults are true/true/false \u2014 so those rows are wrong data rather than a choice.",
      "consequence": "0002 carries `UPDATE trips SET share_photos = true, share_notes = true` with the reasoning in the file. `share_coordinates` is NOT touched: false is correct on both sides. THE DOWN FILE IS NOT AN INVERSE and must say so \u2014 putting everything back to false would leave post-0002 rows wrong in the other direction. ADD THE LEG THE PLAN LACKS: create a trip, apply 0002, read it back. R4's seed makes this moot for seeded rows \u2014 it writes the values from the client document and refuses a non-empty database \u2014 which is why this only bites the trips the slice arc already wrote."
    },
    {
      "id": "DEC-83",
      "status": "ruled",
      "title": "RULED \u2014 photos.place_id / photos.visit_id coherence stays a Go rule, with a comment naming it",
      "reasoning": "EXECUTED: a photograph can name place P while naming a visit belonging to a different place, and the schema accepts it. Every route assumes the pair is coherent. The schema-level fix is a composite FK to `visits (traveller_id, place_id, id)`, but that CHANGES WHAT A VISIT DELETION DOES TO place_id \u2014 which is delete behaviour, and delete behaviour is decided by what the app's sheets promise, not by a DBA.",
      "consequence": "No schema change. A comment on the two columns in 0001 naming this as the SECOND integrity rule deliberately left in Go \u2014 DEC-02's cross-kind id uniqueness is the first \u2014 so the next reader does not read the omission as an oversight, which is exactly how the missing FK indexes got there. TRIGGER for revisiting: a sheet that says what happens to a photograph's pin when its occasion is deleted. Until such a sentence exists there is nothing to implement."
    },
    {
      "id": "DEC-84",
      "status": "ruled",
      "title": "RULED \u2014 S3_PRESIGN_TTL_PUBLIC is 15 minutes, and it IS the revocation window the copy must be written against",
      "reasoning": "The public presign lifetime had no number anywhere, and R2 cannot invent it because four sentences of client copy are written against it. 15 minutes is long enough to read a share page without a mid-scroll failure, short enough that 'stopped' means roughly stopped.",
      "consequence": "`S3_PRESIGN_TTL_PUBLIC = 15m`, `S3_PRESIGN_TTL_PRIVATE` stays short (~2m, DEC-44). THE HONEST SENTENCE, and the copy must say it rather than imply instant revocation: stopping a share stops new links at once, and a photograph already loaded may keep working for up to fifteen minutes. That is FOUR client sentences (D1, D2, D3's 'the shared link stops working', and H1) and it goes in CLIENT-PREREQUISITES as one item, not four. R2's TTL leg was DELETED at v7.1 for being unfalsifiable (it compared the two values to each other); the replacement must assert the PUBLIC path uses the PUBLIC value \u2014 a handler reaching for the private one is the defect, and it would have reddened nothing."
    },
    {
      "id": "DEC-85",
      "status": "ruled",
      "title": "RULED \u2014 share tokens are HASHED at rest, like session tokens; DEC-10's plaintext half is REVERSED",
      "reasoning": "DEC-10 stored the share token in plaintext so `Trip.shareLinkId` could round-trip, and the trade was recorded \u2014 but the security lens noted it was never re-examined, while session tokens carry a whole argued section explaining why they are hashed. Under DEC-67's revoke-and-keep design a dump holds EVERY capability ever issued, live and revoked. Hashing costs that design nothing: store the hash, keep the row.",
      "consequence": "`share_links.token_hash bytea` replacing the plaintext column, sha256 of the raw token, constant-time compared \u2014 the same shape `sessions` already uses. `share_links_token_key` becomes a unique index on the hash. THE COST, and it is real: the server can no longer emit `Trip.shareLinkId`, so a RESTORED device shows a live link as not-shared. That was DEC-10's whole reason for plaintext. It is accepted because the CLIENT HOLDS THE PLAINTEXT \u2014 it minted the token \u2014 so this only bites a device restoring from the server with no local cache, and the fix there is to mint a new link, which H1 already offers. RECORD IT IN CLIENT-PREREQUISITES rather than letting the client phase discover it. The revoked-vs-unknown 404 is unaffected: both still answer identically, and PD-12's equal-work requirement still applies."
    },
    {
      "id": "DEC-86",
      "status": "ruled",
      "title": "RULED \u2014 registration closes after the first traveller",
      "reasoning": "Ruling 3 has always been single-user, and the route already answers 409 when a traveller exists \u2014 but the security lens noted a stranger who registers first on a deployed instance gets an authenticated account, and after d5be39c that account carries a 600/min budget and, from R6, `?photos=delete`.",
      "consequence": "`POST /v1/auth/register` refuses once any traveller row exists. It is one line and it removes the whole question rather than bounding it. NOTE THE ORACLE SHRINKS RATHER THAN GROWS: the 409-on-duplicate the lens flagged as an enumeration surface becomes a 409-on-any-second-registration, which tells an attacker only that the instance is in use \u2014 which the login page already tells them. TRIGGER for revisiting: a second traveller ever being wanted, which is ruling 3 and not this decision."
    },
    {
      "id": "DEC-87",
      "status": "ruled",
      "title": "RULED \u2014 Content-Type is SIGNED into the presigned PUT, so the allowlist reaches the object and not only the row",
      "reasoning": "MEASURED by the storage lens against MinIO: DEC-51's allowlist was described as a defence in three layers and is actually two \u2014 the Go 422 and the 0003 CHECK both constrain the DATABASE ROW, while the uploader sets Content-Type on the PUT and the bucket keeps it. A row reading `image/png`, passing both enforcement points, can address an object the bucket serves as `text/html`. Executed: PUT with `Content-Type: text/html` -> 200, StatObject -> ct=text/html, presigned GET returns it.",
      "consequence": "Sign it, sourced from the SAME validated value the 422 and the CHECK see \u2014 one function, so the three cannot diverge. Measured working: SignedHeaders becomes `content-type;host;x-amz-checksum-sha256`, a mismatched type -> 403, the right one -> 200 with ct preserved. ACCEPT THE WORSE ERROR: a legitimate client that guesses its own MIME type wrongly now gets `403 SignatureDoesNotMatch` rather than a `422` naming the field. That is the trade, and the mitigation is DEC-88's uploadHeaders map \u2014 the client replays what it was given rather than deriving anything."
    },
    {
      "id": "DEC-88",
      "status": "ruled",
      "title": "RULED \u2014 `If-None-Match: *` is signed too: a content address is WRITE-ONCE at the bucket. And the begin response carries an `uploadHeaders` map.",
      "reasoning": "TWO FINDINGS, ONE FIX, and they must land together. (1) The checksum binds the header to the BODY; nothing bound it to the KEY. Executed: a PUT to key <traveller>/<digestA> carrying a self-consistent checksum for different bytes B succeeds, and with versioning off a second PUT at a committed key SILENTLY REPLACES the bytes \u2014 the address stops holding its own contents. (2) A presigned URL whose signature covers extra headers is UNUSABLE unless the caller replays each one exactly, and the begin response carried only `{id, uploadUrl, expiresAt, alreadyExists}`. A client handed only the URL gets 400 on every upload, forever, with no way to derive the base64 header from a hex id.",
      "consequence": "`If-None-Match: *` signed into the PUT. Measured on MinIO: first PUT 200, any second 412 PreconditionFailed with the original bytes intact \u2014 including the replacement attack above. THE RETRY STORY MUST BE WRITTEN DOWN OR THIS IS A REGRESSION: a client re-uploading after an unacknowledged success gets 412, which means 'already there', NOT 'failed'. The commit path treats 412 as success. Put it in CLIENT-PREREQUISITES. AND the begin response gains `uploadHeaders` \u2014 a flat map of exactly the headers the signature covers, values already encoded server-side. The client replays it verbatim and never re-derives an encoding. Add the leg that keeps them honest: the map's key set equals the URL's `X-Amz-SignedHeaders` minus `host`. ALSO FREE, and it should be taken: `StatObject` with `Checksum: true` returns the stored digest, so commit can verify the DIGEST as well as the size at no extra call \u2014 and an object uploaded through the BANNED call has an empty checksum, which makes the ban a RUNTIME guard rather than a grep. Note the flag: without `Checksum: true` the field is silently empty and the check passes nothing."
    },
    {
      "id": "DEC-89",
      "status": "ruled",
      "title": "RULED \u2014 ABSENT MEANS LEAVE ALONE. Every write field becomes a pointer; a nil field is not written.",
      "reasoning": "MEASURED by the safety lens against a real build of HEAD: `PUT /v1/trips/{id}` is SHIPPED and answers HTTP 200 to a body of `{id, name}` \u2014 T4's pencil, which is `renameTrip` and owns the name and nothing else \u2014 while destroying the trip's whole itinerary and both its dates. Executed: three cities and two dates in, then a name-only PUT, then `SELECT count(*) FROM trip_cities` -> 0, response `\"cityIds\":[],\"start\":null,\"end\":null`. `TripWrite.CityIDs` is a bare `[]string` and `checkCityIDs(nil)` loops zero times, so absence and emptiness are the same value. THE AUTHOR ALREADY REASONED THIS OUT ONE LINE ABOVE, for three columns only: share_photos, share_notes and share_coordinates are left out of the SET clause because 'naming them in EXCLUDED-form would silently reset a group this route does not own (SF6) on every rename'. Five columns on the same statement got the other answer. IT IS NOT ONE ROUTE. R6 builds PUT /v1/places/{id} on the same convention over `visits`, whose child is `photos_visit_fk ON DELETE SET NULL`; R7 builds it over photos and over `walks.points`. Measured consequences of each: an empty visits array unfiles 30 photographs across 3 trips at Fushimi alone (95 whole-log) while every count except one stays put; a caption-only PUT that nulls place_id and visit_id passes ALL THREE of the plan's standing guards, because the dangling check sees no dangling reference, the place-without-occasion check sees no place, and the pair-agreement check sees a pair that agrees; and `walks_points_array_ck` does not stop `points='[]'` because an empty array IS an array \u2014 a GPS track is a recording of a day and cannot be retyped.",
      "consequence": "Every field on every write type becomes a pointer, and a nil field is LEFT ALONE rather than written \u2014 exactly as the three sharing flags already are. This makes `createPlace` correct by construction (C1's pin is a wishlist place with no visits) and an accidental re-PUT harmless. THIS IS A FIX TO SHIPPED CODE, so it lands in R1 and not in R6. Three legs, each written against the body the CLIENT actually sends rather than a synthesised whole entity: PUT a trip with three cities then a name-only PUT and assert all three cities and both dates survive; PUT a place with `visits` omitted and assert all 28 visits and all 30 filings survive; PUT a walk with `{dismissed:true}` alone and assert the points are intact. AND ADD THE ONE ASSERTION THE THREE STANDING GUARDS CANNOT MAKE: a count that must not fall. `count(*) WHERE place_id IS NOT NULL` is unchanged across every route in R7 except refile (raises it) and the D1/D2 deletes (lower it by a known amount). Apply R5's 'assert on the surviving row count, never on error/no-error' rule to the routes that do NOT destroy rows."
    },
    {
      "id": "DEC-90",
      "status": "ruled",
      "title": "RULED \u2014 the CLIENT normalises with .toUtc() before encoding; the server REFUSES a zoneless string rather than guessing",
      "reasoning": "MEASURED by the client-fidelity lens, live: `PUT /v1/trips/lens-dates` with `\"start\":\"2027-10-12T00:00:00.000\"` -> `400 {\"code\":\"invalid_body\"}`; the identical body with `...000Z` -> 200. The client's ONLY date input is `pickLogDate`, which is `showDatePicker` returning `DateTime(y,m,d)` in the DEVICE'S LOCAL ZONE; `_addDates` passes it straight through with no conversion (`grep -rn 'toUtc' lib/src/screens/` returns nothing) and `trip.g.dart` writes `toIso8601String()`, which emits NO ZONE SUFFIX for a non-UTC value. `Instant.UnmarshalJSON` parses RFC3339, which requires an offset. So T4's 'Add dates' fails on every request. THE PLAN CANNOT SEE THIS: `client_sample_log.json` was built with `DateTime.utc(...)` and every date string in it carries a Z, so DEC-68's 'byte-identical golden round-trip per date-bearing field' is measured against exactly the data that cannot expose it.",
      "consequence": "The client's API layer calls `.toUtc()` on every DateTime before encoding. Trip dates are calendar days and the log already treats them as midnight UTC everywhere else. NOTE THAT THIS ALSO CHANGES WHAT logbook.json HOLDS LOCALLY \u2014 it is a local-format change as well as a wire one, and it belongs in CLIENT-PREREQUISITES with that said out loud. The server keeps refusing: add a leg asserting a zoneless date is REFUSED and not reinterpreted \u2014 guessing at a string's meaning is the class of thing `logbookFormatVersion` exists to refuse. GOLDEN LEGS NEED INPUTS NO FIXTURE CONTAINS: one zoneless string, and one six-digit fraction. `instantLayout` is `.000` and Go TRUNCATES, while Dart writes six digits whenever `microsecond != 0` \u2014 which `logbookNow()` always has on the VM, so `Photo.filedLater` and a minted `Visit.at` can both carry six. Millisecond precision on the wire is the contract and the client truncates before sending, so the round trip IS byte-identical. `expiresAt` is outside DEC-68's six fields (it emits NINE digits today) and must not be 'fixed' into an Instant."
    },
    {
      "id": "DEC-91",
      "status": "ruled",
      "title": "RULED \u2014 the emitted Trip gains `\"shared\": bool`, derived from share_links, so revocation survives DEC-85",
      "reasoning": "DEC-85 names the cost as 'a RESTORED device shows a live link as not-shared'. MEASURED by the client-fidelity lens, the cost is larger and is not restricted to restores. DEC-32's write response is a whole Trip the phone SPLICES into its cached log, and with tokens hashed at rest that Trip carries `shareLinkId: null` \u2014 measured live on `PUT /v1/trips/lens-dates`. So an ordinary RENAME, an 'Add dates', or a T5 itinerary change on a SHARED trip overwrites the local plaintext token with null, and the client holds the only copy. H1 then renders no URL, and BOTH 'Copy link' and 'Stop sharing' go inert (`onPressed: !trip.isShared || _busy ? null : _stop`), while the row in `share_links` is un-revoked and `GET /l/{token}` still serves it. The user loses the capability AND the only control that revokes it, from an action that has nothing to do with sharing. `Trip.withName`'s own doc states the invariant this breaks verbatim: 'renaming a trip you have shared must not quietly kill the URL somebody is holding.' THE PLAN'S GUARD IS BLIND TO IT \u2014 the leg is 'the write's answer spliced into the cached document decodes equal to the next whole read', and the next whole read is null too.",
      "consequence": "Emit `\"shared\": bool` on the Trip, as `EXISTS (SELECT 1 FROM share_links WHERE traveller_id=$1 AND trip_id=t.id AND revoked_at IS NULL)`. It leaks no capability, it restores 'Stop sharing' from any device, and it is one correlated subquery on a query already ordered by id. The client's `Trip.isShared` becomes `shared` rather than `shareLinkId != null`, and H1 gains a THIRD STATE it does not have today: shared, but this device does not hold the link \u2014 'New link' is the way back, which H1 already offers. Additive with a default, so it does not move the format version by the client's own rule. The splice must also be a MERGE that preserves `shareLinkId` rather than a replacement, and the leg must be rewritten against a trip that HAS a live link \u2014 the current one uses a trip that does not, which is why it passes. AND RE-DERIVE DEC-67 while here: its history argument rests on 'DEC-10 stores the token in plaintext', which DEC-85 has just made false."
    },
    {
      "id": "DEC-92",
      "status": "ruled",
      "title": "RULED \u2014 a pg_dump target lands in R4, and `make slice` stops being able to destroy the live volume",
      "reasoning": "MEASURED by the safety and operations lenses, independently, and they agree. After R4 the compose volume holds the ONLY copy of the record \u2014 the plan's own premise is 'PostgreSQL is the record and the phone is a cache'. Across all 261 KB of plan-v7: `pg_dump` 0 hits, `backup` 0, `rotat` 0. Across CLAUDE.md, every `restore` hit is about mutation-testing file restores. AND THE TOOLING DESTROYS IT. `scripts/slice-arc.sh:389-390` runs `docker compose down -v` as step A0 against the DEFAULT project (`name: travellog`, volume `pgdata`) \u2014 the same volume `make up`, `make migrate` and `make seed` write. No prompt, no `--yes`, no guard variable; the only protection is a comment in the Makefile. FIVE OF THE EIGHT STEPS' acceptance checks read `make seed && make check && make slice`, so the documented procedure teaches a developer that seeding and then wiping is normal \u2014 on a stack that will later hold real photographs. The arc already runs two of its five phases under their own COMPOSE_PROJECT_NAME (lines 563 and 636); the main phase using the live project is the inconsistency, not the pattern.",
      "consequence": "THREE THINGS, IN R4, WHICH IS WHERE THE VOLUME STOPS BEING DISPOSABLE. (1) A `make backup` target: `docker compose exec -T postgres pg_dump -Fc -U travellog travellog > backups/travellog-<date>.dump`, custom format so `pg_restore` survives a minor version bump, keep 7. (2) ONE RESTORE REHEARSAL, run once, with its output recorded in EVIDENCE.md \u2014 a backup nobody has restored is not a backup, and this project already holds that standard for everything else. (3) `make slice` runs under its own COMPOSE_PROJECT_NAME, matching what the arc's own testdb and healthcheck phases already do, and refuses the live project unless `SLICE_DESTROY_VOLUME=1`. Reorder the five acceptance checks so `make slice` does not read as 'seed then wipe'. WHEN R2 LANDS, THE ORDER IS FIXED: back up the BUCKET first and the DATABASE second. A dump newer than the bucket copy references objects that were never copied; a bucket copy newer than the dump only leaves unreferenced garbage."
    },
    {
      "id": "DEC-93",
      "status": "ruled",
      "title": "RULED \u2014 Walk.points is CAPPED LOW, a few hundred, and the track stays inside the log document",
      "reasoning": "MEASURED by the performance lens, and the fixture could not have answered it: the sample log's TWO walks carry SEVEN POINTS BETWEEN THEM, so every size argument in plan-v7 was calibrated on a log with no real track in it. Marshalled through the shipped types: 100 points 5,226 B; 1,000 points 51,004 B; 3,600 points 183,277 B; 21,600 points 1,099,390 B \u2014 OVER the 1 MiB body ceiling, which is a six-hour walk at 1 Hz. Uncapped, the user's LONGEST walk is the one write that silently cannot be saved. At the whole-log scale it is worse: tracks, not photographs, are the read's real growth term. Same photograph count, 10x scale \u2014 the fixture's 3-4 point walks read in 12.54 ms / 991,675 B; 1,800-point walks read in 37.09 ms / 2,821,305 B. And points COMPRESS BADLY, because they are arrays of distinct floats: 4.4-6.2x against 15.6-17.5x for the rest of the document.",
      "consequence": "The cap is a few hundred points and the client SIMPLIFIES the track before saving \u2014 C2 draws a polyline and does not need every fix. Enforce it in `ValidateWalk` with its own 422 NAMING THE FIELD, not by `http.MaxBytesReader`, which answers `ErrBodyTooLarge` with no field name. This closes three questions at once and they should be written down as closed: the walk write fits the 1 MiB ceiling with two orders of magnitude to spare; DEC-31's whole-log read stays small, so the trigger for per-resource ETags stays far away; and a track stays IN the log document rather than becoming a media object like a photograph. The client's own decimation is a CLIENT-PREREQUISITES item \u2014 the server refusing an over-long track is not the same as the phone knowing to shorten one."
    },
    {
      "id": "DEC-94",
      "status": "ruled",
      "title": "RULED \u2014 gzip middleware lands now, and WriteTimeout stops being the only bound on the read",
      "reasoning": "MEASURED by the performance lens against a real server holding a plausible ten-year log (350 trips, 14,200 photographs, emitted body 11,102,597 bytes). `GET /v1/logbook` answers HTTP 200 with a VALID ETag and a SILENTLY TRUNCATED body when body/link-speed exceeds WriteTimeout. Reproduced three times: at 400 kB/s, 8,371,312 bytes received, curl exit 18, `json.load` -> 'Unterminated string starting at char 8371258', last bytes on disk `{\"lat\":35.0409862745072,\"lng`. At 500 kB/s: 9,631,256 bytes, code 200. At 200 kB/s: 5,622,648 bytes, code 200. Unthrottled the same request is 151.9 ms and complete. There is no Content-Encoding, no Accept-Encoding handling, no Vary, and no ceiling on the READ \u2014 the 1 MiB ceiling is on the WRITE, and this is what that asymmetry costs. Caddy would take most of it away and Caddy is deferred to next_slice, so today the Go server is the edge and there is nothing in front of it.",
      "consequence": "Both halves, and they are independent. (1) gzip: measured on the real fixture, `gzip -6` takes 85,422 -> 5,479 (15.6x); through the emitter, level 1 takes 99,271 -> 6,806 in 0.3 ms, and level 1 on the 11.1 MB log gives 2,084,727 bytes (5.3x) in 47.2 ms. Honour Accept-Encoding and SET VARY, or the next cache in front of this serves compressed bytes to a client that did not ask. (2) Raise WriteTimeout, or make it a function of body size, and say the ceiling out loud \u2014 at 15s the server is promising that no log will ever exceed 15 seconds of the client's downlink and nothing enforces that. COMPRESSION ALONE DOES NOT REMOVE THE CLASS; it moves the threshold by 5-15x. DEC-93's cap removes the largest growth term, which is why these two rulings are worth reading together."
    },
    {
      "id": "DEC-95",
      "status": "ruled",
      "title": "RULED \u2014 restart: always on both services, log rotation on both, and the sizing numbers written down",
      "reasoning": "MEASURED by the operations lens as a controlled pair, because the distinction is invisible by inspection. `unless-stopped` does not restart a container stopped from OUTSIDE, and that decision persists across daemon restart and host reboot. Executed: a container that dies on its own reaches `restarting restarts=6` in 20s; a container killed by a human sits at `exited restarts=0` indefinitely. Confirmed on the real service \u2014 `docker kill` left Postgres exited and still exited 100+ seconds later, while the API sat at `healthy` and every request 500'd. So `docker stop` to take a dump, then a reboot, leaves an unattended box permanently down with nothing reporting it. The line is in the compose file uncommented, which is unusual for that file. Two more measured absences alongside it. NO LOG ROTATION: `LogConfig.Config` is empty on both containers and there are no daemon-level log-opts, so json-file runs uncapped \u2014 3,066,153 bytes over 21.35 idle hours = 3.4 MB/day = 1.26 GB/year, of which 15,151 of 15,960 lines (94.9%) are the healthcheck talking to itself. NO RESOURCE LIMITS: the API's steady working set after Argon2 has run once is 140.5 MiB against 12.42 MiB idle, and it does NOT come back down \u2014 Go's scavenger does not return it promptly \u2014 so each +1 on ARGON2_MAX_CONCURRENT is +64 MiB of STEADY working set. R2's MinIO measures 214.7 MiB idle, larger than the API and Postgres together.",
      "consequence": "`restart: always` on both, with the reason in the compose file's own voice: unless-stopped remembers a human decision that the human will not remember. `logging: {driver: json-file, options: {max-size: 10m, max-file: 3}}` on both, AND stop access-logging /healthz at Info \u2014 the disk cost is survivable anywhere; the signal cost is a 20:1 dilution of the one file you read at 3am. `mem_limit` per service, so the OOM-killer's choice is yours rather than 'whichever has the largest RSS' \u2014 which, after R2, is MinIO and not the API. Put the three numbers (140.5 / 214.7 / Postgres at its default 128 MB shared_buffers) into BEFORE-A-PUBLIC-DEPLOY.md as the sizing derivation: a 1 GiB VPS is roughly half-spent before any data."
    },
    {
      "id": "DEC-96",
      "status": "ruled",
      "title": "RULED \u2014 a request gets a bound, a statement gets a bound, and a dependency outage answers 503 with Retry-After",
      "reasoning": "MEASURED by the operations lens, and it corrects a belief the plan holds: R1 adds `lock_timeout` to the MIGRATOR and the plan treats that as closing the database lens's stall finding. IT DOES NOT. `lock_timeout` bounds how long the migration WAITS; it does nothing for the requests queued behind the migration's pending ACCESS EXCLUSIVE lock. Executed against a synthetic 0002 of exactly R1's shape: one `GET /v1/logbook` returned curl http=000 at its own 30s limit \u2014 NO STATUS, NO BODY, NO ERROR. Ten concurrent requests all returned 000 after 18.8s, `pg_stat_activity` showing 9 backends active/Lock. `/healthz` answered 200 in 4.7 ms throughout, because it pings and never touches `trips`; once the pool was exhausted it flipped to 503 after stalling its full 2.004s budget \u2014 so /healthz's answer is a fact about the POOL, not about whether the API can serve. Docker reported `healthy` through all of it. `httpx.Timeout` is written and has ZERO production call sites; the app DSN carries no `statement_timeout` and no `idle_in_transaction_session_timeout`; `db.Conn()` has no acquire deadline. SEPARATELY: with Postgres killed, every route answers `500 {\"code\":\"internal\"}` with no Retry-After. A 500 tells a client the server has a bug \u2014 do not retry, the request is poison \u2014 when the truth is 'the dependency is down, try again shortly'. It is also unanswerable afterwards: a 500 count conflates handler bugs with outages.",
      "consequence": "Wire `httpx.Timeout` with a REQUEST_TIMEOUT variable at 15s, and put `statement_timeout=15s` and `idle_in_transaction_session_timeout=60s` on cmd/api's DSN IN THE SAME STEP as R1's lock_timeout \u2014 not conditionally in R4 item 8, because a number chosen and written beats a number deferred. Keep R1's lock_timeout; it is the third of three and not the whole. Map driver-level connectivity failures (pgconn connect errors, sql.ErrConnDone, a pool-acquire deadline) to 503 with `Retry-After: 5`, leaving 500 for genuine handler faults. The taxonomy in `internal/httpx/errors.go` already exists; this is one code and one branch. One leg holds a lock and asserts the request gets a BOUNDED 503 rather than silence; one leg kills Postgres and asserts 503 with Retry-After. AND RAISE THE HEALTH START PERIOD ABOVE migrateTimeout. The Dockerfile writes out three nesting budgets (2s < 3s < 4s < 5s) and there is a fourth that does not nest: the server does not listen until migrations finish, migrateTimeout allows 120s, and the health budget expires at 3s + 12x5s = 63s. Measured: `health=unhealthy` at t=61s while a CORRECT migration was still waiting for its lock and went on to apply at t=98s. `start_period: 150s` costs nothing, because a check that passes during the start period marks the container healthy at once."
    },
    {
      "id": "DEC-97",
      "status": "ruled",
      "title": "RULED \u2014 `make seed` refuses when ANY TRAVELLER ROW EXISTS, and its passphrase is generated per run",
      "reasoning": "DEC-86 was ruled after R4 was written, and together they lock the owner out of their own deployment with a published credential. R4 item 5 seeds 'ONE traveller with a known address and passphrase, printed on completion'. DEC-86 closes `POST /v1/auth/register` once any traveller row exists. R4's refusal is scoped to 'a non-empty LOG' \u2014 a freshly migrated database IS empty, so the guard does not fire. `make up && make seed` is the obvious first thing an operator types, and the result is a deployment whose only account has a passphrase printed in a terminal and checked into a fixture, carrying a 600/min budget, with registration now closed so the real traveller can never create theirs. The converse is unspecified too: after the real user registers but before they write anything, it is not stated whether 'non-empty log' counts a traveller row. DEC-75 states the intent correctly \u2014 'never run at boot, never in production' \u2014 and R4 implements a guard that does not deliver it.",
      "consequence": "ONE PREDICATE, and it is the same one DEC-86 puts on register: refuse when any traveller row exists. It covers both directions and removes the ambiguity about a registered-but-unwritten account. Keep PD-02's required `-dsn` with no environment fallback. Add a second guard the plan does not have \u2014 an explicit marker such as `--i-know-this-is-a-dev-database` \u2014 because the DSN alone cannot tell a dev database from a production one. Generate the passphrase per run so a fixture cannot become a shipped credential, and print the DSN and the traveller id in the REFUSAL as well as the success, so an operator who has pointed at the wrong database learns it from the refusal. ALSO STATE THE SEED'S OWN SEAM: the ten-table load is one transaction (correct) and the two PNG uploads happen before it (DEC-78), so a load that dies at table seven rolls back the database and leaves the objects. That is intended and content addressing makes the re-upload idempotent \u2014 but it must be written down, because once `-sweep-media` has a grace window, an uploaded-not-committed object is indistinguishable from an orphan."
    },
    {
      "id": "DEC-98",
      "status": "ruled",
      "title": "RULED \u2014 the media store CREATES THE BUCKET at boot, which also gives it the connectivity check Postgres already has",
      "reasoning": "MEASURED by the storage and operations lenses independently: NOTHING CREATES THE BUCKET. `MakeBucket` 0 hits and `mc mb` 0 hits across 41 mentions of `bucket` in plan-v7. Executed against the official image at a pinned tag with a fresh volume and no init: `/minio/health/live` 200, `/minio/health/ready` 200, `ls /data` -> `.minio.sys` only, zero buckets. `MINIO_DEFAULT_BUCKETS` is a Bitnami variable, not a MinIO one. AND NEITHER HEALTHCHECK CAN SEE IT. `up -d --wait` reports three healthy services against a MinIO that cannot store anything, and because presigning is OFFLINE SIGNING, `PresignPut` against a non-existent bucket SUCCEEDS and returns a perfectly-formed URL \u2014 the failure surfaces on the phone as `NoSuchBucket` on the PUT. That is exactly the fail-open shape R2 is careful about for the checksum and the length, arriving through a different door.",
      "consequence": "`MakeBucket` + `BucketExists` in the media store's constructor at boot, rather than an `mc` one-shot service \u2014 it is the same argument `run()` already makes for its Postgres ping ('a failure here is a real misconfiguration and the process should not come up pretending otherwise'), and R2 currently gives media only a nil-check at wiring time. Make the definition of done assert the BUCKET and not the process: a round-trip PUT+GET inside the `up --wait` arc. Add `depends_on: minio: condition: service_healthy` to api. AND PIN THE SHIPPED MinIO TAG. R2 pins MinIO for the TEST compose file and says nothing about the shipped one \u2014 pinned backwards, in a repo whose compose file already carries a long comment about why an unpinned Postgres tag would silently orphan a named volume."
    },
    {
      "id": "DEC-99",
      "status": "ruled",
      "title": "RULED \u2014 a no-transaction migration must be PROVED re-runnable, and R3 is where the proof lands",
      "reasoning": "MEASURED by the operations lens against the real Migrator. A `-- migrate:no-transaction` file that fails part way is an UNRECOVERABLE BOOT LOOP: the runner applies statements 1..i, writes no `schema_migrations` row, and every subsequent boot re-runs from statement 1 \u2014 failing on an already-applied statement with a DIFFERENT error that HIDES THE ORIGINAL FAULT. `restart: unless-stopped` (now `always`, DEC-95) retries this forever. Executed with a three-statement 0003: run 1 reported `statement 3 ... ERROR: division by zero`; runs 2 and 3, byte-identical file, both reported `statement 2 ... ERROR: relation \"probe_half_applied\" already exists`. The actual fault is no longer reachable from any run after the first. The sharp part is that NOTHING ENFORCES the re-runnability the runner's own comment demands ('such a file must be written to be re-runnable') \u2014 no test, no lint, no acceptance check. R3 is the step that introduces 0003, and CREATE INDEX CONCURRENTLY is precisely the statement class that needs the directive AND that leaves an INVALID index behind on failure.",
      "consequence": "Land the guard WITH R3, not with the incident. (a) A test that runs every no-transaction migration TWICE against the same database and requires the second run to be clean \u2014 that is what 're-runnable' means and it is mechanically checkable. (b) An `IF NOT EXISTS` / `DROP ... IF EXISTS` requirement stated in the file header and greppable. (c) A documented recovery in the same step: the exact psql to inspect `schema_migrations`, plus the `DROP INDEX` for the INVALID index CREATE INDEX CONCURRENTLY leaves. (d) Make the failure message carry the version and the STATEMENT TEXT, not just the ordinal, so log line one is enough."
    },
    {
      "id": "DEC-100",
      "status": "ruled",
      "title": "RULED \u2014 TouchSession leaves the traveller's advisory lock and becomes granular",
      "reasoning": "MEASURED by the performance lens with pg_stat_statements reset around exactly one 304: NINE database round trips, FIVE of them to stamp a timestamp. `begin`, `SELECT pg_advisory_xact_lock(hashtextextended($1::uuid::text,0))`, `SELECT 1 FROM travellers`, `UPDATE sessions SET last_used_at`, `commit`, then the session lookup, then the read snapshot's three. Total server exec 0.176 ms against a 3.0-4.0 ms wall clock \u2014 so it is round trips, not work. It also SERIALISES against the phone's own in-flight writes through the same advisory lock. tx.go's comment argues at length that sessions must not bump `logbook_version`, which is correct and is a DIFFERENT QUESTION from whether they should take the write lock.",
      "consequence": "Take TouchSession out of `WithTravellerLock`: it writes one row keyed by session id, and the lock buys it nothing the row lock does not. The existence check the wrapper performs for it is a whole extra row read per request, and the UPDATE's own `WHERE traveller_id` already answers that question. Make it granular: write `last_used_at` only when the stored value is more than a few minutes old, which turns a per-request write into a per-hour one. Nothing in this project depends on it being exact."
    },
    {
      "id": "DEC-101",
      "status": "ruled",
      "title": "RULED \u2014 the access log gains microseconds, a traveller id and a route pattern, and every 500 emits exactly one ERROR line",
      "reasoning": "MEASURED by the operations lens over 21.35 hours of the live stack, 15,960 lines: `durationMs` is an int64 of MILLISECONDS, so 15,151 of them read `durationMs:0` and NO LATENCY QUESTION IS ANSWERABLE FROM THESE LOGS AT ALL. The only non-zero values in the whole sample were 4 and 9. Three more, each cheap now and expensive later: no traveller id on the access line (costs nothing at one traveller, and it is the field that has to thread through middleware); `path` is the RAW path rather than the route pattern, so once `/v1/trips/{id}` exists nothing aggregates; and nothing MANDATES that a handler returning an error \u2014 as opposed to panicking \u2014 logs an ERROR line with the requestId and the driver detail. `auth_handlers.go` does it and it was observed working under a killed Postgres, joined by requestId. R5-R8 write fourteen more handlers with no rule stated, and a 500 whose only line is the access line is a 3am dead end.",
      "consequence": "`durationUs` instead of `durationMs` \u2014 one word, and it is the difference between having latency data and not. `travellerId` on the request-scoped logger where auth already resolves it (the redactor is keyed on substrings of token/passphrase/authorization, so an id is safe). The MATCHED PATTERN alongside the path. And into the definition of done: every 500 emits EXACTLY ONE ERROR line carrying requestId and the underlying error, asserted per handler package by a leg that COUNTS LINES rather than greps for one. NO METRICS AND NO TRACING, and this is a decision rather than an absence: one traveller, one box, one process \u2014 a Prometheus scrape and a dashboard would be more moving parts than the thing they watch, and a structured access log answers every question a single-user deployment can ask once it logs microseconds. The trigger for revisiting is a SECOND BOX or a SECOND TRAVELLER, not traffic volume."
    },
    {
      "id": "DEC-102",
      "status": "ruled",
      "title": "RULED \u2014 the size premise is 99,271 bytes, the read is TEN queries, and both numbers get corrected where they are written",
      "reasoning": "MEASURED by the performance lens. (a) The emitted body at fixture scale is 99,271 bytes, NOT the 85,422 every size argument in the plan carries. 85,422 is the CLIENT's format-1 document, whose assets are 31-32 character bundle paths; DEC-46 replaced those with 64-hex object ids (+32 bytes x 284 photographs plus covers) and DEC-40 stamps version 2. Every size argument is ~16% optimistic and the drift is STRUCTURAL \u2014 it grows with the photograph count. This is the number DEC-31 rests on. (b) One whole-log read is TEN SELECTs, not the six `logbook_store.go`'s own doc comment claims. pg_stat_statements after one read: photos, visits, trip_cities, places, cities, trips, walk_points, walks, logbook_version, traveller name \u2014 ten, each `calls = 1`. read_tx.go's 'six lists' is a fair description of the DOCUMENT; the store's 'six queries' is a COUNT and it is wrong. (c) Three date columns are scanned into `sql.NullTime` and read unconditionally \u2014 `v.At`, `p.TakenAt`, `w.RecordedOn`, with `.Valid` never checked. Correct today because all three are NOT NULL, but the constraint is doing 100% of the work, and the same file is careful with `instantOrNil` and with `if lat.Valid && lng.Valid`. The asymmetry reads as an oversight.",
      "consequence": "Correct both numbers where they are written, with the derivation rather than the figure alone \u2014 'the fixture emits 99,271 bytes through THIS BUILD', and ten queries, which is also what makes the 3 ms 304 legible (9 round trips at ~0.3 ms over 0.176 ms of server work). Scan the three dates into `time.Time` directly, so the driver errors on a NULL instead of the emitter inventing `0001-01-01T00:00:00.000Z` \u2014 which `DateTime.parse` accepts happily and every screen renders as a year-1 date. One line each, no behaviour change today. AND STATE THE GO/SQL SPLIT so nobody optimises the wrong half: at 100x, 37.06 ms of a 61.20 ms read is Postgres and 24.1 ms is row-to-struct scanning through database/sql. The query plans are already sane and the planner is right to decline indexes on the small tables."
    }
  ],
  "gate_l12": {
    "status": "CLEARED",
    "cleared_on": "2026-08-22",
    "cleared_by": "the human, in response to an explicit approval request",
    "what_was_approved": "The Phase 1 deliverables of go_backend.md L9-L10, as rendered and read: the class diagram (11 entities, the full relationship table with ON DELETE behaviour and the delete-sheet sentence each implements, and the fields the backend needs that the client lacks) and six sequence diagrams (register, sign in, cold start and revalidation, a write, a photograph end to end, the public share read), each with trigger, request payload, expected response and error states.",
    "artefact_sha256": "f5dab13de264fbf246d7c1746fa08ce334545eadbf70bd1d361ef7706b8b647f",
    "approved_with_these_known_open": [
      "media_objects referenced without a foreign key, validated in Go",
      "country flattened into cities rather than a lookup table",
      "citext for the email column, which needs a Postgres extension",
      "the register and sign-in request bodies, which the spec does not specify"
    ],
    "note": "Recorded with its date, its scope and the artefact's hash BECAUSE the incident this gate exists for was a plan describing artefacts as 'approved' that no human had seen. An approval with no scope is the same defect wearing a tick. If the diagrams change materially, this clearance does not carry over \u2014 re-approve."
  }
}